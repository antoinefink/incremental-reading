{
  "name": "interleave",
  "version": "0.0.0",
  "private": true,
  "description": "Interleave — a desktop-first, local-first incremental reading application.",
  "packageManager": "pnpm@9.12.1",
  "engines": {
    "node": ">=22.13.1 <23",
    "pnpm": ">=9.12.1 <10"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.5",
    "typescript": "^5.7.3"
  }
}
