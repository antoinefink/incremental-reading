{
  "name": "@interleave/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"@interleave/web: no unit tests yet (UI tests land with T003+)\"",
    "lint": "echo \"@interleave/web: linted at the workspace root via Biome\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@interleave/core": "workspace:*"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
