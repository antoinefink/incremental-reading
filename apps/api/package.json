{
  "name": "@interleave/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "echo \"@interleave/api: no Hono server yet (wired in T051)\"",
    "build": "echo \"@interleave/api: no build step yet (wired in T051)\"",
    "test": "echo \"@interleave/api: no tests yet (wired in T002)\"",
    "lint": "echo \"@interleave/api: no lint yet (wired in T002)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
